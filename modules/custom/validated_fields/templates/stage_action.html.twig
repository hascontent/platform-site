{#
/**
 * @file stage_action.html.twig
 * Default theme implementation to present Stage action data.
 *
 * This template is used when viewing Stage action pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_stage_action()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('stage_action') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
